#compdef grb

_grb_commands() {
  _values "grb" "create" "publish" "rename" "delete" "track" "help" "explain"
}

_grb_local_branches() {
  compadd `git branch --no-color | cut -c3-`
}

if (( CURRENT == 2 )); then
  _grb_commands
else
  _grb_commands
  _grb_local_branches
fi