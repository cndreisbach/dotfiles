function_exists() {
    declare -f -F $1 > /dev/null
    return $?
}

if [ -f ~/.env ]; then
  . ~/.env
fi

if [ -f ~/.aliases ]; then
  . ~/.aliases
fi

for file in $(ls ~/.bash); do
  . ~/.bash/$file
done

. ~/dotfiles/lib/z/z.sh
#. ~/dotfiles/lib/autoenv/activate.sh

command -v xrdb > /dev/null
if [ $? -eq 0 ] && [ -f ~/.Xdefaults ]; then
  xrdb -merge ~/.Xdefaults
fi
